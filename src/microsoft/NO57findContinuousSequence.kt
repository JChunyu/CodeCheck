package microsoft

class NO57findContinuousSequence {
    // n(first + last) = target * 2
    // last - first = n
    // (last - first)(first + last) = target * 2
    // 2last * first - first * first + last * last = target * 2
    // last * last - first * first = target * 2
    fun findContinuousSequence(target: Int): Array<IntArray> {
        return arrayOf(intArrayOf())
    }
}